(let ((freq 100)) ;; set initial frequency to 100

  ;; repeat the sequence 100 times
  (repeat 100 (sequence  
    ;; play freq tone for 100 ms
    (tone freq 100)
    (print freq)

    ;; add 10 to freq
    (setf freq (+ freq 10))
  ))

)